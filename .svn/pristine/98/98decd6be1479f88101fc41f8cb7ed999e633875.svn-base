import React from 'react';
import { useNavigate } from 'react-router-dom';
import Notification from '../../features/notification/Notification';

const Header = () => {
  const navigate = useNavigate();

  return (
    // <header> 태그는 부모(DoctorCalendarPage)에 있으므로 내부 div부터 시작
    <div className="flex items-center justify-between w-full h-full px-6">
      
      {/* 좌측: 로고/타이틀 */}
      <div 
        className="flex items-center cursor-pointer" 
        onClick={() => navigate('/')} // 로고 클릭 시 홈으로
      >
        <h1 className="text-xl font-bold text-slate-900 tracking-tight">
          <span className="text-blue-600">SB</span> 정형외과
        </h1>
      </div>

      {/* 우측: 액션 버튼들 */}
      <div className="flex items-center gap-3">
        
        {/* 홈 버튼 */}
        <button 
          className="p-2 text-slate-500 rounded-full hover:bg-slate-100 transition-colors"
          aria-label="홈"
          onClick={() => navigate('/')}
        >
          <svg 
            width="24" 
            height="24" 
            viewBox="0 0 24 24" 
            fill="none" 
            stroke="currentColor" 
            strokeWidth="1.5" // JSX에서는 strokeWidth로 표기
            strokeLinecap="round" 
            strokeLinejoin="round"
          >
            <path d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"></path>
            <polyline points="9 22 9 12 15 12 15 22"></polyline>
          </svg>
        </button>

        {/* 알림 버튼 (빨간 점 표시) */}
        {/* <button 
          className="relative p-2 text-slate-500 rounded-full hover:bg-slate-100 transition-colors"
          aria-label="알림"
        >
          <svg 
            width="24" 
            height="24" 
            viewBox="0 0 24 24" 
            fill="none" 
            stroke="currentColor" 
            strokeWidth="1.5" 
            strokeLinecap="round" 
            strokeLinejoin="round"
          >
            <path d="M18 8A6 6 0 0 0 6 8c0 7-3 9-3 9h18s-3-2-3-9"></path>
            <path d="M13.73 21a2 2 0 0 1-3.46 0"></path>
          </svg>
          <span className="absolute top-2 right-2 w-2 h-2 bg-red-500 rounded-full border border-white"></span>
        </button> */}

        {/* 알림 */}
        <Notification/>

        {/* 구분선 (선택 사항) */}
        <div className="w-px h-8 bg-slate-200 mx-1"></div>

        {/* 프로필 이미지 */}
        <div className="flex items-center ml-1 cursor-pointer">
          <img 
            src="https://placehold.co/36x36" 
            alt="프로필" 
            className="w-9 h-9 rounded-full border border-slate-200 object-cover hover:ring-2 hover:ring-blue-100 transition-all" 
          />
        </div>
      </div>
    </div>
  );
};

export default Header;